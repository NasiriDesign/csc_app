version: '3.8'

services:
  go:
    image: go-image
    container_name: app
    restart: always
    ports: 
      - 3000:3000
    networks:
      - wp_myNetwork

networks:
  wp_myNetwork:
    external: true
